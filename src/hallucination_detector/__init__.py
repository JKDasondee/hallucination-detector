from hallucination_detector.core import Detector, detect
from hallucination_detector.models import Claim, DetectionResult

__version__ = "0.1.0"
__all__ = ["detect", "Detector", "Claim", "DetectionResult"]
